#include <stdio.h>
extern int myadd(int a, int b);

int main(void)
{
    int a, b;
    printf("input : \n");
    scanf("%d", &a);
    scanf("%d", &b);

    int c=myadd(a, b);

    printf("sum : %d\n", c);

    return 0;
}
